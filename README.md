# EhsanParsed

This repository takes Ehsan's code and modularizies it so it's easier to understand and run in multiple environments

### Run all code using .venv python environment
To activate the environmen (from the EhsanParsed directory): 

`source .venv/bin/activate`

To deactivate:

`deactivate`